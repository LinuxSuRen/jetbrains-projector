apiVersion: v1
kind: Service
metadata:
  name: {{ include "ks-goland.fullname" . }}
  labels:
    {{- include "ks-goland.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ks-goland.selectorLabels" . | nindent 4 }}
