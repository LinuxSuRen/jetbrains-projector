apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ks-goland.fullname" . }}-test-connection"
  labels:
    {{- include "ks-goland.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ks-goland.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
